AnalysisNetwork <- function(x, analysis_method) {
  # Find the clusters in one projection of the bipartite graph by the given adjacent matrix of the projection.
  #
  # Args:
  #   x: adjacent matrixn of the projection of the original matrix.
  #   analysis_method: Selecting the method for network analysis of the cluster. (like centerality, H-index, betweeness)
  #
  # Returns:
  #   an array with the score of each node with given measurement.
  
  return()
}