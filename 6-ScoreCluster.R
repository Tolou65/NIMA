ScoreCluster <- function(x, score_method) {
  # Find the scores of clusters in one projection of the bipartite graph by use of the given adjacent matrix.
  #
  # Args:
  #   x: adjancy matrix of the projected bipartite graph.
  #   score_method: Selecting the scoring method for clustering evaluation. (like silhouette, Calinski_Harabasz, David-Bouldin Index)
  #
  # Returns:
  #   the score of the executed clustering.
  
  return()
}