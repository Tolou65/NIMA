ImputeMissingValue <- function(x, method) {
  # Impute the missing value in the given data matrix.
  #
  # Args:
  #   x: inpute matrix with missing values.
  #   method: Specify the method of imputation like mean, MICE, MissForest.
  #
  # Returns:
  #   matrix without missing values..
  
  return()
}